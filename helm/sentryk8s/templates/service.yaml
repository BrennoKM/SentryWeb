apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentryk8s.fullname" . }}
  labels:
    {{- include "sentryk8s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentryk8s.selectorLabels" . | nindent 4 }}
